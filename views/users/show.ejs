<!DOCTYPE html>
<html>
<head>
  <title>User Details</title>
</head>
<body>
  <h1>User Details</h1>
  <h3><%= user.name %></h3>
  <p>Age: <%= user.age %></p>
  <p>Email: <%= user.email %></p>
  <a href="/crud/<%= user._id %>/edit">Edit</a>
  <form method="POST" action="/crud/<%= user._id %>/delete" onsubmit="return confirm('Are you sure you want to delete this user?')">
    <button type="submit">Delete</button>
  </form>
</body>
</html>